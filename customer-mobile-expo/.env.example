# Copy to .env and set this to your PC's LAN IP so your phone (running Expo Go)
# can reach the backend. Find your IP on Windows with: ipconfig
# Look for "IPv4 Address" under your active WiFi adapter, e.g. 192.168.1.42
EXPO_PUBLIC_API_BASE_URL=http://192.168.1.42:8000
